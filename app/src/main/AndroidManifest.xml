<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- In-app updater: launches the system package installer on a downloaded APK. Required
         since API 26 for any app-initiated install that isn't through an app store — the user
         still has to explicitly allow it per-app (Settings > Special app access > Install
         unknown apps), this permission just lets the app ask; it grants nothing by itself. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/Theme.Atlas"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Atlas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Playback: Media3 MediaSessionService, foregroundServiceType=mediaPlayback gives
             lock-screen/notification controls and screen-off playback. -->
        <service
            android:name=".playback.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- Downloads: plain foreground service so the queue keeps processing when the app
             is backgrounded. dataSync is the closest fit of Android's foreground service
             types for "downloading files in the background" pre-API34 downloadManager-style
             services were folded into. -->
        <service
            android:name=".data.download.DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Voice Isolation: CPU-bound ONNX inference over a full track can take minutes,
             mediaProcessing (API 34+) is the correct foreground service type for exactly this;
             ignored harmlessly on older API levels where it isn't defined. -->
        <service
            android:name=".data.voiceisolation.VoiceIsolationService"
            android:exported="false"
            android:foregroundServiceType="mediaProcessing" />

        <!-- Exposes the downloaded update APK (app-private storage) to the system package
             installer via a content:// Uri — installers on API 24+ can't be handed a raw
             file:// path from another app's private storage directly. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
